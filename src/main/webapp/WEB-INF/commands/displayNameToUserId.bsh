displayNameToUserId(username) {
  userid = null;

  if (!empty(username)) {
    i = username.indexOf('>');
    if ((i >= 0) && (i<username.length()-1)) {
      userid = username.substring(i+1);
    } else {
      userid = username;
    }
  }

  return userid;
}